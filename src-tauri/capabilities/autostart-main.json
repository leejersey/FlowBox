{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "autostart-main",
  "description": "enables autostart permissions for the main window",
  "windows": [
    "main"
  ],
  "permissions": [
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
